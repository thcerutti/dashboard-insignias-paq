const baseUrl = "https://insignias-paq.vercel.app";

const constants = {
  endpoints: {
    educandos: `${baseUrl}/educandos`,
    insignias: `${baseUrl}/insignias`,
    detalheEducando: `${baseUrl}/educando`,
    detalheInsignia: `${baseUrl}/insignia`,
  },
};

export default constants;
